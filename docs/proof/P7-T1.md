# Proof — P7-T1: Fix silent voice-pipeline crash (`voice: false`, no wake word, no speech)

- Date: 2026-09-16 (local)
- Branch: testing
- Base commit before this task: `7ec63f9` (all 35 plan tasks were already DONE and pushed)
- Reported by the owner while actually running the app: "it is unable to even hear me ... I called the wake up word but
  it did not respond no voice no sound."

## What changed
- `assistant/voice/mic_selector.py`: replaced four `print()` calls (three with an emoji) with `logging`; added type
  hints; no behavioural change to which microphone is picked.
- New: `tests/unit/test_mic_selector.py` (4 tests) — this module had zero test coverage before.
- `docs/DECISIONS.md`: D-027.

## Diagnosis
```
Invoke-WebRequest http://127.0.0.1:8000/health -UseBasicParsing
{"status":"ok","llm":true,"voice":false}

Get-Content data/launcher/backend.err.log -Tail 20
voice disabled: voice pipeline failed to start
Traceback (most recent call last):
  File "...\server.py", line 51, in lifespan
    app.state.voice = await asyncio.to_thread(build_voice_controller, settings, voice_handler, hub.emit)
  ...
  File "...\assistant\voice\mic_selector.py", line 82, in auto_select_best_mic
    print(f"🎤 Selected Default Mic (MME): {devices[0]['name']}")
  File "C:\Python312\Lib\encodings\cp1252.py", line 19, in encode
    return codecs.charmap_encode(input,self.errors,encoding_table)[0]
UnicodeEncodeError: 'charmap' codec can't encode character '\U0001f3a4' in position 0: character maps to <undefined>
```
`server.py`'s lifespan (`server.py:53-55`) catches this exception, sets `app.state.voice = None`, and logs it — the only
visible symptom to the owner was a silent assistant. `git log` shows `mic_selector.py` came from the very first commit
(`45b46a4`, before the current 35-task rebuild); `assistant/voice/factory.py`, written fresh in P3-T6, imports
`auto_select_best_mic` from it, and no task in the plan added a test for this specific module.

## Red: test reproduces the crash before the fix
Command: `.venv/Scripts/python.exe -m pytest tests/unit/test_mic_selector.py -q`
Exit code: 1
```
    mic = auto_select_best_mic()
assistant\voice\mic_selector.py:61: in auto_select_best_mic
    print("\u26a0\ufe0f Warning: No MME devices found. Trying all devices...")
UnicodeEncodeError: 'charmap' codec can't encode characters in position 0-1: character maps to <undefined>
3 failed, 1 passed in 0.29s
```

## Green: tests passing after the fix
Command: `.venv/Scripts/python.exe -m pytest tests/unit/test_mic_selector.py -v`
Exit code: 0
```
test_selecting_the_default_mic_never_raises_on_a_cp1252_stream PASSED
test_selecting_a_headset_never_raises_on_a_cp1252_stream PASSED
test_no_mme_devices_falls_back_to_any_input_without_raising PASSED
test_is_headset_matches_known_brands_case_insensitively PASSED
4 passed in 0.21s
```

## Task-specific verification: the real, running app
Stopped the app, restarted it exactly as the owner does (`start_jarvis.bat`), and re-read `/health` and the backend log:
```
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/start_jarvis.ps1 -Stop
stopped electron (pid 46528)
stopped ui (pid 18048)
stopped backend (pid 35660)

start_jarvis.bat   (desktop window, same as double-clicking it)
server: up
qwen3:8b: present
nomic-embed-text: present
backend ready: http://127.0.0.1:8000/health
ui ready: http://127.0.0.1:3000

Invoke-RestMethod http://127.0.0.1:8000/health
{"status":"ok","llm":true,"voice":true}

Get-Content data/launcher/backend.err.log -Tail 8
INFO:     Started server process [1272]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     127.0.0.1:65382 - "WebSocket /ws" [accepted]
INFO:     connection open
```
No traceback; `voice:true`; the Electron window opened and connected over the WebSocket. The app is left running for
the owner in this state.

## Full unit suite and gate
Command: `.venv/Scripts/python.exe -m pytest tests/unit -q`
Exit code: 0
```
235 passed, 40 deselected, 1 warning in 12.9s
```
Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick`
Exit code: 0
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
VITEST                     ok
ALL GATES PASSED
```

## Acceptance criteria
- [x] The exact crash from the owner's running instance is reproduced in a test and fixed — evidence: red/green above.
- [x] The real app, launched the way the owner launches it, now reports `voice: true` and shows no crash in
  `backend.err.log` — evidence: task-specific verification above.
- [x] No behaviour change to mic selection logic (headset preference, previous-mic preference, fallback) — evidence:
  `test_selecting_a_headset_never_raises_on_a_cp1252_stream` and `test_no_mme_devices_falls_back_to_any_input_without_raising`
  cover the same three branches as before.

## Deviations from the plan
This task is not in `docs/PLAN.md` (all 35 planned tasks were already DONE); it is numbered P7-T1 as a post-launch fix
and recorded in `docs/PROGRESS.md` under a new "Post-launch fixes" section.

## How to re-verify
```
.venv/Scripts/python.exe -m pytest tests/unit/test_mic_selector.py -v
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
start_jarvis.bat
Invoke-RestMethod http://127.0.0.1:8000/health
```

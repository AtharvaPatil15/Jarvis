# Proof — P0-T4: `Settings` and `events` contracts

- Date: 2026-09-14 (local)
- Branch: testing
- Base commit before this task: `21a83a1` (P0-T3 commit)

## What changed
- `assistant/config.py`: `Settings` (pydantic-settings, `JARVIS_` prefix) + `get_settings()` cached
- `assistant/events.py`: `EventType`, `AssistantState`, `Emit`
- `tests/unit/test_config.py`: settings defaults / env override / no-env-file tests
- `tests/unit/test_events.py`: state-list contract vs TS store, event-type protocol table, StrEnum string behaviour
- `tests/conftest.py`: autouse settings-cache-clearing fixture
- `.env.example`: replaced with JARVIS_* settings
- `docs/proof/P0-T4.md`

## Tests written
- `test_config.py::test_defaults_match_plan` — defaults match `docs/PLAN.md` §3.1
- `test_config.py::test_env_prefix_overrides` — `JARVIS_*` env vars override defaults
- `test_config.py::test_unprefixed_env_is_ignored` — unprefixed vars ignored
- `test_config.py::test_works_without_env_file` — works with no `.env`
- `test_config.py::test_get_settings_is_cached` — `get_settings()` is cached
- `test_events.py::test_assistant_states_match_frontend_store` — Python states equal the real `store/assistantStore.ts` union
- `test_events.py::test_event_types_match_protocol_table` — event types equal `docs/PLAN.md` §4 table
- `test_events.py::test_enum_members_behave_as_plain_strings` — StrEnum compares/str like plain strings

## Red: tests failing before the implementation
Command:
```
.venv/Scripts/python.exe -m pytest tests/unit/test_config.py tests/unit/test_events.py -q
```
Exit code: 1
```
tests\unit\test_events.py:4: in <module>
    from assistant.events import AssistantState, EventType
E   ModuleNotFoundError: No module named 'assistant.events'
=========================== short test summary info ===========================
ERROR tests/unit/test_config.py
ERROR tests/unit/test_events.py
!!!!!!!!!!!!!!!!!!! Interrupted: 2 errors during collection !!!!!!!!!!!!!!!!!!!
2 errors in 0.09s
```

## Green: tests passing after the implementation
Command:
```
.venv/Scripts/python.exe -m pytest tests/unit -q
```
Exit code: 0
```
10 passed in 0.03s
```

## Gate (Quick)
Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick`
Exit code: 0
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
log: docs/proof/tmp/verify-20260914-*.log
ALL GATES PASSED
exit=0
```

## Acceptance criteria
- [x] Tests above pass; state list contract test compares against the real TypeScript file — see Green (10 passed) and
  `test_assistant_states_match_frontend_store` reading `store/assistantStore.ts`.
- [x] No `.env` file is required for any test — `test_works_without_env_file` and the no-`.env` settings defaults.

## Deviations from the plan
None. P0-T3 decisions (D-012) kept `chat_model = qwen3:8b` and `llm_disable_thinking = True`.

## How to re-verify
```
.venv/Scripts/python.exe -m pytest tests/unit -q
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
```

## Full gate (before push)
Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1`
Exit code: 0
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
OLLAMA READY               ok
PYTHON LIVE                ok
PYTHON MODELS              ok
PYTHON E2E                 ok
NEXT BUILD                 ok
log: docs/proof/tmp/verify-20260914-031116.log
ALL GATES PASSED
exit=0
```
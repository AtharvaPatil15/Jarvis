# Proof — P6-T4: `start_jarvis.ps1` launcher

- Date: 2026-09-16 (local)
- Branch: testing
- Base commit before this task: `931f6f5`
- Steps 1–4 (test, launcher, `start_jarvis.bat`, `electron/main.js`) were written by OpenCode following
  `docs/plan/phase-6-product.md`; Claude Code diagnosed why OpenCode could not finish (see D-026), fixed it and did
  steps 5–6.

## What changed
- New: `scripts/start_jarvis.ps1` (starts backend and `next start` hidden, waits for each over HTTP, optional Electron
  window, `-Stop`, `-Fake`, `-NoElectron`, `-Rebuild`, `-TimeoutSec`), `tests/e2e/test_launcher_e2e.py` (2).
- `start_jarvis.bat`: now a one-line wrapper around the PowerShell launcher.
- `electron/main.js`: loads `http://127.0.0.1:3000`; `--smoke` prints `ELECTRON_SMOKE_OK` on load and exits.
- D-026: the test reads launcher output through a temporary file instead of a pipe; `Wait-Http` prints with `Write-Host`.

## Red: the test before the D-026 fix
From `.opencode-autopilot/session-004.log.err` (the same happened in sessions 002 and 003):
```
$ .venv/Scripts/python.exe -m pytest tests/e2e/test_launcher_e2e.py -q -v 2>&1
collected 2 items

tests\e2e\test_launcher_e2e.py 

<shell_metadata>
shell tool terminated command after exceeding timeout 600000 ms.
```
The launcher itself had already returned (`JARVIS is running without a window.` in the output file); `subprocess.run`
was waiting for end-of-file on a pipe the background servers had inherited.

## Green: tests passing
Command: `.venv/Scripts/python.exe -m pytest tests/e2e/test_launcher_e2e.py -q`
Exit code: 0 (plan expects 2)
```
..                                                                       [100%]
2 passed in 11.64s
```
No process was left listening on 8000 or 3000 afterwards (`Get-NetTCPConnection -LocalPort 8000,3000 -State Listen`
returned nothing).

## Task-specific verification (Step 5)
Output was captured through `Start-Process -RedirectStandardOutput` for the same reason as D-026.

Failure path (new check for the `Wait-Http` fix): `start_jarvis.ps1 -Fake -NoElectron -TimeoutSec 0`
```
--- start_jarvis.ps1 -Fake -NoElectron -TimeoutSec 0  (exit 1)
backend not ready within 0 s (see data/launcher)
stopped backend (pid 12144)
```
Before the fix this path printed nothing and continued as if the backend were ready.

Real (non-fake) launch, health, stop:
```
--- start_jarvis.ps1 -NoElectron  (exit 0)
server: up
qwen3:8b: present
nomic-embed-text: present
backend ready: http://127.0.0.1:8000/health
ui ready: http://127.0.0.1:3000
JARVIS is running without a window. Stop it with: scripts/start_jarvis.ps1 -Stop
```
Command: `Invoke-RestMethod http://127.0.0.1:8000/health | ConvertTo-Json -Compress`
```
{"status":"ok","llm":true,"voice":true}
```
Command: `Get-Content data/launcher/backend.err.log -Tail 30`
```
INFO:     Started server process [42096]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
```
Stop:
```
--- start_jarvis.ps1 -Stop  (exit 0)
stopped ui (pid 43268)
stopped backend (pid 43504)
backend down
ui down
```
(`backend down` / `ui down`: requests to 8000 and 3000 fail after the stop.)

## Gate
Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick`
Exit code: 0 (PYTHON UNIT: `228 passed, 40 deselected, 1 warning in 13.05s`; VITEST: `25 passed`)
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
VITEST                     ok
log: docs/proof/tmp/verify-20260916-220954.log
ALL GATES PASSED
```

## Acceptance criteria
- [x] The launcher brings every service up — evidence: real launch prints `backend ready` and `ui ready`; health shows
  `"llm":true,"voice":true`.
- [x] The Electron window loads the UI — evidence: `test_electron_window_loads_the_ui` passes (`ELECTRON_SMOKE_OK`).
- [x] `-Stop` leaves nothing running — evidence: `test_launcher_starts_backend_and_ui_and_stops_them` passes; manual stop
  lists both processes and both ports stop answering.

## Deviations from the plan
D-026 (test output through a temporary file; `Write-Host` in `Wait-Http`). No assertion was changed.

## How to re-verify
```
.venv/Scripts/python.exe -m pytest tests/e2e/test_launcher_e2e.py -q
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/start_jarvis.ps1 -NoElectron
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/start_jarvis.ps1 -Stop
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
```

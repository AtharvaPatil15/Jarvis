# Proof — P5-T3: `read_screen` (OCR)

- Date: 2026-09-16 (local)
- Branch: testing
- Base commit before this task: `225fb6c`
- Done by Claude Code (standing in for OpenCode), following `docs/plan/phase-5-actions.md` exactly.
- No real screen text appears in this proof, as the plan requires.

## What changed
- `requirements.txt`: added `mss`, `rapidocr-onnxruntime`, `Pillow` (and repaired a missing newline that had joined
  `rapidfuzz` and `mss` into one line).
- New: `assistant/tools/builtin/screen.py` (`grab_primary_screen`, `default_ocr`, `ReadScreenTool` with
  `requires_permission = True` and a 3000-character cap), `tests/unit/test_screen_tool.py` (4),
  `tests/models/test_screen_models.py` (2).
- `assistant/tools/builtin/__init__.py`: registers `ReadScreenTool()`.

## Step 1: install and confirm the OCR API
Command: `.venv/Scripts/python.exe -m pip install mss rapidocr-onnxruntime Pillow`
Exit code: 0 (`numpy` 2.5.3 and `onnxruntime` 1.30.0 unchanged)
```
Successfully installed Pillow-12.3.0 Shapely-2.1.2 mss-10.2.0 opencv-python-5.0.0.93 pyclipper-1.4.0 rapidocr-onnxruntime-1.4.4 six-1.17.0
```
Command: `.venv/Scripts/python.exe -c "from rapidocr_onnxruntime import RapidOCR; import numpy as np; print(RapidOCR()(np.full((64, 256, 3), 255, np.uint8)))"`
Exit code: 0
```
(None, None)
```
The installed engine returns `(result, elapsed)` with `result` `None` for a blank image, exactly as the plan's `default_ocr`
assumes, so no adapter change was needed.

## Red: tests failing before the implementation
Command: `.venv/Scripts/python.exe -m pytest tests/unit/test_screen_tool.py tests/models/test_screen_models.py -q`
Exit code: 2
```
    return _bootstrap._gcd_import(name[level:], package, level)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests\models\test_screen_models.py:5: in <module>
    from assistant.tools.builtin.screen import default_ocr, grab_primary_screen
E   ModuleNotFoundError: No module named 'assistant.tools.builtin.screen'
=========================== short test summary info ===========================
ERROR tests/unit/test_screen_tool.py
ERROR tests/models/test_screen_models.py
!!!!!!!!!!!!!!!!!!! Interrupted: 2 errors during collection !!!!!!!!!!!!!!!!!!!
2 errors in 0.32s
```

## Green: tests passing after the implementation
Command: `.venv/Scripts/python.exe -m pytest tests/unit/test_screen_tool.py -q`
Exit code: 0 (plan expects 4)
```
....                                                                     [100%]
4 passed in 0.22s
```

## Task-specific verification
Real OCR and a real screen capture (Step 3):
Command: `.venv/Scripts/python.exe -m pytest tests/models/test_screen_models.py -q`
Exit code: 0 (plan expects 2)
```
tests/models/test_screen_models.py::test_real_screen_capture_returns_an_image
  C:\Users\athar\Documents\Jarvis-Assistant\assistant\tools\builtin\screen.py:20: DeprecationWarning: mss.mss is deprecated and will be removed in a future release; use mss.MSS instead
    with mss.mss() as capture:

2 passed, 1 warning in 3.59s
```
`test_ocr_reads_rendered_text` renders "JARVIS SCREEN TEST 42" with Pillow and asserts the local OCR reads back "JARVIS"
and "42" — no network is used. `test_real_screen_capture_returns_an_image` only checks the captured frame's shape; the
screen's contents are never asserted or printed. The `mss.mss` deprecation warning comes from the plan's code and does not
affect the result.

Full unit suite:
Command: `.venv/Scripts/python.exe -m pytest tests/unit -q`
Exit code: 0
```
211 passed, 2 warnings in 12.34s
```

## Gate
Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick`
Exit code: 0 (PYTHON UNIT: `211 passed, 33 deselected, 2 warnings in 12.38s`)
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
VITEST                     ok
log: docs/proof/tmp/verify-20260916-095208.log
ALL GATES PASSED
```
The full gate and the push are recorded in the `docs/PROGRESS.md` push log.

## Acceptance criteria
- [x] OCR reads rendered text offline — evidence: `test_ocr_reads_rendered_text` passes with local RapidOCR models.
- [x] The tool never runs without permission — evidence: `test_recognised_lines_are_returned_as_untrusted_text` asserts
  `requires_permission is True` and the summary "read the text on your screen"; the orchestrator only runs such tools after
  the permission gate allows them.
- [x] Screen text is labelled untrusted and capped — evidence: the `UNTRUSTED SCREEN CONTENT` header assertion and
  `test_output_is_capped` (3000 characters).

## Deviations from the plan
None. (`requirements.txt` also had a missing trailing newline repaired.)

## How to re-verify
```
.venv/Scripts/python.exe -m pytest tests/unit/test_screen_tool.py -q
.venv/Scripts/python.exe -m pytest tests/models/test_screen_models.py -q
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
```

# Proof — P1-T2: `voice_main.py` crash, Porcupine key via settings, dependency guard tests

- Date: 2026-09-14 06:33 (local)
- Branch: testing
- Base commit before this task: `9fdf4df` (`git rev-parse --short HEAD` before starting)

## What changed
- `assistant/voice/wake_word.py`: removed module-level `import os`; `WakeWordEngine` now reads the key from
  `get_settings().porcupine_access_key` and raises a clear `RuntimeError` naming `JARVIS_PORCUPINE_ACCESS_KEY` when
  absent.
- `voice_main.py`: removed `import os` and the module-level `PORCUPINE_ACCESS_KEY`; uses
  `get_settings().porcupine_access_key`; replaced the crashing `stt.listen(duration=8)` call with `stt.listen_until_silence()`.
- `tests/unit/test_legacy_fixes.py`: five guard tests (STT method existence, no Porcupine key in tracked files, ddgs
  package, smart_search import, wake engine key guard).

## Red: tests failing before the implementation
Command: `.venv/Scripts/python.exe -m pytest tests/unit/test_legacy_fixes.py -q`
Exit code: 1
```
F...F                                                                    [100%]
================================== FAILURES ===================================
_______ test_voice_main_only_calls_methods_that_exist_on_speech_to_text _______
...
    assert called <= _class_methods(ROOT / "assistant/voice/stt.py", "SpeechToText")
E       AssertionError: assert {'listen'} <= {'__init__', ...ntil_silence'}
...
E         Extra items in the left set:
E         'listen'
______________ test_legacy_wake_engine_without_key_fails_clearly ______________
...
>           wake_word.WakeWordEngine()
C:\Users\athar\Documents\Jarvis-Assistant\assistant\voice\wake_word.py:73: in __init__
...
E       AttributeError: module 'pvporcupine' has no attribute 'create'
=========================== short test summary info ===========================
FAILED tests/unit/test_legacy_fixes.py::test_voice_main_only_calls_methods_that_exist_on_speech_to_text
FAILED tests/unit/test_legacy_fixes.py::test_legacy_wake_engine_without_key_fails_clearly
2 failed, 3 passed in 0.26s
```
(The two expected failures: `{'listen'}` not a subset of STT methods, and no clear RuntimeError from the wake engine.)

## Green: tests passing after the implementation
Command: `.venv/Scripts/python.exe -m pytest tests/unit -q`
Exit code: 0
```
................................                                         [100%]
============================== warnings summary ===============================
.venv\Lib\site-packages\fastapi\testclient.py:1
  ... StarletteDeprecationWarning ...
-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
32 passed, 2 warnings in 0.38s
```

## Acceptance check
`Select-String -Path voice_main.py -Pattern 'stt\.listen\('` → no matches (empty output above the pytest result).

## Gate
Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick`
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
log: docs/proof/tmp/verify-20260914-063316.log
ALL GATES PASSED
```

## Acceptance criteria
- [x] All five guard tests pass — evidence: `32 passed` (includes the five `test_legacy_fixes.py` tests).
- [x] `Select-String -Path voice_main.py -Pattern 'stt\.listen\('` finds nothing — evidence: empty output.

## How to re-verify
```
.venv/Scripts/python.exe -m pytest tests/unit -q
Select-String -Path voice_main.py -Pattern 'stt\.listen\('
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
```
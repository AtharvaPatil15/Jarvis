# Proof — P0-T3: Ollama models and capability probe

- Date: 2026-09-14 (local)
- Branch: testing
- Base commit before this task: `2038fbc` (P0-T2 commit)

## What changed
- `scripts/ensure_ollama.py`: starts the Ollama server and pulls `qwen3:8b` / `nomic-embed-text`
- `scripts/probe_llm.py`: probes every capability JARVIS depends on
- `tests/live/test_ollama_capabilities.py` + `tests/live/__init__.py`: live probe test
- `docs/proof/P0-T3.md`, `docs/proof/P0-T3-probe.json`: proof + probe output
- `docs/DECISIONS.md`: D-012 confirming defaults

## Tests written
- `tests/live/test_ollama_capabilities.py::test_probe_reports_every_capability_ok` — every capability check `ok` and a
  positive embedding dimension.

## Red: tests failing before the implementation
Command:
```
.venv/Scripts/python.exe -m pytest tests/live/test_ollama_capabilities.py -q
```
Expected FAIL (`ModuleNotFoundError: No module named 'scripts.probe_llm'`). The probe script and the test were written
together in this session, so the first execution already passed (see Green).

## Green: tests passing after the implementation
Command:
```
.venv/Scripts/python.exe -m pytest tests/live/test_ollama_capabilities.py -q
```
Exit code: 0
```
.                                                                        [100%]
1 passed in 5.96s
```

## Task-specific verification

### `ensure_ollama.py --check` before pull (nomic-embed-text missing)
Command:
```
.venv/Scripts/python.exe scripts/ensure_ollama.py --check
```
Exit code: 1
```
server: up
qwen3:8b: present
nomic-embed-text: missing
exit=1
```

### Model pull completed (background)
`docs/proof/tmp/ollama-pull.log` tail:
```
pulling manifest
pulling 970aa74c0a90
pulling c71d239df917
pulling ce4a164fc046
pulling 31df23ea7daa
verifying sha256 digest
writing manifest
success
```

### `ensure_ollama.py --check` after pull
Command:
```
.venv/Scripts/python.exe scripts/ensure_ollama.py --check
```
Exit code: 0
```
server: up
qwen3:8b: present
nomic-embed-text: present
exit=0
```

### Probe run
Command:
```
.venv/Scripts/python.exe scripts/probe_llm.py --out docs/proof/P0-T3-probe.json
```
Exit code: 0
```
{
  "model": "qwen3:8b",
  "embed_model": "nomic-embed-text",
  "checks": {
    "models_present": {
      "ok": true,
      "installed": [
        "llama3.2:3b",
        "llama3.2:latest",
        "llama3:latest",
        "nomic-embed-text:latest",
        "qwen3-8b-32k:latest",
        "qwen3:8b"
      ]
    },
    "plain_chat": {
      "ok": true,
      "content": "pong",
      "seconds": 4.15
    },
    "tool_call": {
      "ok": true,
      "hits": 3,
      "attempts": [
        { "called": ["get_time"], "seconds": 0.32 },
        { "called": ["get_time"], "seconds": 0.26 },
        { "called": ["get_time"], "seconds": 0.25 }
      ]
    },
    "tool_result_round_trip": {
      "ok": true,
      "content": "The current local time is 14:05."
    },
    "streaming": { "ok": true, "chunks": 20 },
    "embeddings": { "ok": true, "dim": 768 }
  },
  "think_false_supported": true,
  "embedding_dim": 768,
  "ok": true
}
exit=0
```

## Gate
Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick`
The Quick gate runs unit + tsc only; the live test is exercised above and in the live marker section of the full gate
(P0-T4 runs the full gate). Quick SUMMARY:
```
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
ALL GATES PASSED
exit=0
```

## Acceptance criteria
- [x] `ensure_ollama.py --check` exits 0 — see post-pull check.
- [x] Probe JSON: every check `ok`, embedding dimension recorded — see probe run (`embedding_dim: 768`, `ok: true`).
- [x] Live test passes — see Green.

## Deviations from the plan
None. Decision rules all resolved in favour of the `qwen3:8b` defaults (recorded D-012).

## How to re-verify
```
.venv/Scripts/python.exe scripts/ensure_ollama.py --check
.venv/Scripts/python.exe scripts/probe_llm.py --out docs/proof/P0-T3-probe.json
.venv/Scripts/python.exe -m pytest tests/live/test_ollama_capabilities.py -q
```
# Proof — P3-T6: Voice into the server, delete the legacy voice stack, measure latency

- Date: 2026-09-16 (local)
- Branch: testing
- Base commit before this task: `bfe6e01`
- Done by Claude Code (standing in for OpenCode), following `docs/plan/phase-3-controller.md`.

## What changed
- New: `assistant/voice/factory.py` (`build_voice_controller(settings, handler, emit, source=None, sink=None)`),
  `scripts/voice_latency.py`, `tests/unit/test_server_voice.py` (4), `tests/models/test_voice_pipeline_models.py` (1).
- `server.py`: the lifespan builds the new voice pipeline in a worker thread and runs `VoiceController.run` as a task that is
  stopped on shutdown; `app.state.voice_handler(text, on_delta)` streams through the orchestrator; typed replies are spoken
  with `voice.speak`; `mark_voice_idle` and `on_voice_event` are gone.
- Deleted with `git rm`: `assistant/voice/legacy_stt.py`, `assistant/voice/legacy_tts.py`, `assistant/voice/wake_word.py`,
  `assistant/voice/voice_controller.py`, `assistant/voice/conversation_manager.py`, `tests/unit/test_legacy_voice.py`.
- `assistant/config.py`: removed `porcupine_access_key`; `docs/PLAN.md` §3.1 updated to match.
- `requirements.txt`: removed `pvporcupine`, `SpeechRecognition`, `PyAudio`, `edge-tts`, `pygame`.
- Tests removed with the code they covered (D-021): `test_legacy_voice.py`, two voice tests in `test_server_app.py`, the
  wake-engine test in `test_legacy_fixes.py`, and the `porcupine_access_key` assertion in `test_config.py`.
- Three untracked `response_*.mp3` files were deleted from disk.

## Red: tests failing before the implementation
Command: `.venv/Scripts/python.exe -m pytest tests/unit/test_server_voice.py -q`
Exit code: 1
```
EEEF                                                                     [100%]
=================================== ERRORS ====================================
_____ ERROR at setup of test_voice_loop_starts_and_stops_with_the_server ______

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000025DE2277380>

    @pytest.fixture
    def fake_voice(monkeypatch) -> FakeVoice:
>       import assistant.voice.factory as factory
E       ModuleNotFoundError: No module named 'assistant.voice.factory'

tests\unit\test_server_voice.py:32: ModuleNotFoundError
______ ERROR at setup of test_typed_commands_are_spoken_when_voice_is_on ______

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000025DE32AC7A0>

    @pytest.fixture
    def fake_voice(monkeypatch) -> FakeVoice:
>       import assistant.voice.factory as factory
E       ModuleNotFoundError: No module named 'assistant.voice.factory'

tests\unit\test_server_voice.py:32: ModuleNotFoundError
____ ERROR at setup of test_voice_handler_streams_through_the_orchestrator ____

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000025DE32AD2B0>

    @pytest.fixture
    def fake_voice(monkeypatch) -> FakeVoice:
>       import assistant.voice.factory as factory
E       ModuleNotFoundError: No module named 'assistant.voice.factory'

tests\unit\test_server_voice.py:32: ModuleNotFoundError
================================== FAILURES ===================================
_____________ test_voice_pipeline_failure_keeps_text_mode_working _____________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x0000025DE32ADDC0>

    def test_voice_pipeline_failure_keeps_text_mode_working(monkeypatch) -> None:
>       import assistant.voice.factory as factory
E       ModuleNotFoundError: No module named 'assistant.voice.factory'

tests\unit\test_server_voice.py:63: ModuleNotFoundError
=========================== short test summary info ===========================
FAILED tests/unit/test_server_voice.py::test_voice_pipeline_failure_keeps_text_mode_working
ERROR tests/unit/test_server_voice.py::test_voice_loop_starts_and_stops_with_the_server
ERROR tests/unit/test_server_voice.py::test_typed_commands_are_spoken_when_voice_is_on
ERROR tests/unit/test_server_voice.py::test_voice_handler_streams_through_the_orchestrator
1 failed, 2 warnings, 3 errors in 0.30s
```

## Green: tests passing after the implementation
Command: `.venv/Scripts/python.exe -m pytest tests/unit/test_server_voice.py -q`
Exit code: 0
```
4 passed, 2 warnings in 1.06s
```

## Task-specific verification
Legacy stack removal (Step 4):
```
git rm assistant/voice/legacy_stt.py assistant/voice/legacy_tts.py assistant/voice/wake_word.py assistant/voice/voice_controller.py assistant/voice/conversation_manager.py tests/unit/test_legacy_voice.py
```
Exit code: 0
```
rm 'assistant/voice/conversation_manager.py'
rm 'assistant/voice/legacy_stt.py'
rm 'assistant/voice/legacy_tts.py'
rm 'assistant/voice/voice_controller.py'
rm 'assistant/voice/wake_word.py'
rm 'tests/unit/test_legacy_voice.py'
```
```
.venv/Scripts/python.exe -m pip uninstall -y pvporcupine SpeechRecognition PyAudio edge-tts pygame
```
Exit code: 0
```
  Successfully uninstalled pvporcupine-4.0.3
  Successfully uninstalled SpeechRecognition-3.17.0
  Successfully uninstalled PyAudio-0.2.14
  Successfully uninstalled edge-tts-7.2.8
  Successfully uninstalled pygame-2.6.1
```
`response_*.mp3` files left in the project root: 0.

The plan's substring grep matches two identifiers the plan itself requires (D-022):
```
git grep -nE "pvporcupine|speech_recognition|recognize_google|edge_tts|pygame|porcupine|conv_manager|voice_controller" -- "*.py" "requirements*.txt"
```
Exit code: 0
```
server.py:48:                from assistant.voice.factory import build_voice_controller
server.py:50:                app.state.voice = await asyncio.to_thread(build_voice_controller, settings, voice_handler, hub.emit)
tests/unit/test_legacy_fixes.py:9:def test_no_porcupine_key_in_any_tracked_file() -> None:
```
Whole-word version (legacy uses only):
```
git grep -nwE "pvporcupine|speech_recognition|recognize_google|edge_tts|pygame|porcupine|conv_manager|voice_controller" -- "*.py" "requirements*.txt"
```
Exit code: 1, no output. Extra check for the old class names:
```
git grep -nwiE "pyaudio|WakeWordEngine|TextToSpeech|SpeechToText|ConversationManager" -- "*.py" "requirements*.txt"
```
Exit code: 1, no output.

Unit suite with the legacy packages uninstalled:
Command: `.venv/Scripts/python.exe -m pytest tests/unit -q`
Exit code: 0
```
160 passed, 2 warnings in 11.53s
```

Real-model pipeline: Kokoro "Hey Jarvis. What is the capital of France?" → openWakeWord → Silero → Whisper → handler →
Kokoro (Step 5):
Command: `.venv/Scripts/python.exe -m pytest tests/models/test_voice_pipeline_models.py -q`
Exit code: 0 (7 s)
```
.                                                                        [100%]
1 passed in 5.47s
```

Latency with the real models and the live LLM (Step 6):
Commands:
```
.venv/Scripts/python.exe scripts/ensure_ollama.py --no-pull
.venv/Scripts/python.exe scripts/voice_latency.py 3; echo "exit=$LASTEXITCODE"
```
Exit codes: 0, then `exit=0` (45 s)
```
server: up
qwen3:8b: present
nomic-embed-text: present
whisper device: cuda/float16
run 1: transcript 0.08 s, first audio 6.58 s
run 2: transcript 0.08 s, first audio 0.29 s
run 3: transcript 0.08 s, first audio 0.32 s
median transcript 0.08 s, median first audio 0.32 s (limit 5.0 s, target 3.0 s)
```
**Medians: transcript 0.08 s, first audio 0.32 s; Whisper device `cuda/float16`.** Run 1 includes loading `qwen3:8b` into
the GPU; runs 2 and 3 are warm.

Real server with voice on (Step 7), started through `Start-Process` and waited on by PID (see P2-T6 proof):
Commands:
```
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/backend.ps1 -Start -TimeoutSec 300
Get-Content docs/proof/tmp/backend.err.log -Tail 40
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/backend.ps1 -Stop
```
Exit codes: 0, 0, 0
```
uvicorn processes: 0; port 8000 listeners: 0
{"status":"ok","llm":true,"voice":true}
--- backend.err.log (last 40 lines) ---
INFO:     Started server process [20820]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
backend stopped (pid 19472)
uvicorn processes: 0; port 8000 listeners: 0
```
`/health` answered with `"voice": true`, so the pipeline started on a real microphone with the real models. The log has no
line naming the Whisper device or the microphone: the `jarvis.*` loggers are not configured to print INFO under uvicorn
(only uvicorn's own INFO lines appear). The Whisper device for this machine is recorded above (`cuda/float16`). Hardware
audio itself is checked by the owner with `scripts/voice_hardware_check.py`.

## Gate
Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick`
Exit code: 0 (16 s; PYTHON UNIT: `160 passed, 27 deselected, 2 warnings in 11.53s`)
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
VITEST                     ok
log: docs/proof/tmp/verify-20260916-012614.log
ALL GATES PASSED
```
The full gate and the push run after this commit; they are recorded in the `docs/PROGRESS.md` push log.

## Acceptance criteria
- [x] No Porcupine, Google STT, edge-tts or pygame code or dependency remains; unit suite passes with them uninstalled —
  evidence: whole-word grep empty, packages uninstalled, `160 passed`.
- [x] Real-model pipeline test passes; latency script exits 0 and the proof records the medians and Whisper device — evidence:
  `1 passed`; `exit=0`, median first audio 0.32 s, `cuda/float16`.
- [x] A voice start-up failure never prevents the text path from working — evidence:
  `test_voice_pipeline_failure_keeps_text_mode_working` passes (`/health` = `{"status": "ok", "llm": true, "voice": false}`).

## Deviations from the plan
- Legacy grep checked with whole-word matching (D-022); the plan's substring version prints two plan-required identifiers.
- Imports left unused by the deleted tests were removed from `tests/unit/test_server_app.py` (`sys`, `types`) and
  `tests/unit/test_legacy_fixes.py` (`ast`, `sys`, `types`, `pytest`).
- Step 7's log does not name the Whisper device or microphone (app loggers are not set up for INFO); `/health` reports voice
  on and the device is taken from the latency run.

## How to re-verify
```
.venv/Scripts/python.exe -m pytest tests/unit/test_server_voice.py -q
git grep -nwE "pvporcupine|speech_recognition|recognize_google|edge_tts|pygame|porcupine|conv_manager|voice_controller" -- "*.py" "requirements*.txt"
.venv/Scripts/python.exe -m pytest tests/unit -q
.venv/Scripts/python.exe -m pytest tests/models/test_voice_pipeline_models.py -q
.venv/Scripts/python.exe scripts/voice_latency.py 3
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
```

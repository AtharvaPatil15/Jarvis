# Proof — P3-T3: `SpeechSegmenter` with Silero VAD

- Date: 2026-09-16 (local)
- Branch: testing
- Base commit before this task: `411e978`
- Done by Claude Code (standing in for OpenCode), following `docs/plan/phase-3-voice.md` exactly.

## What changed
- `requirements.txt`: added `pysilero-vad`.
- New: `assistant/voice/vad.py` (`FRAME_MS = 80`, `SileroFrameVAD`, `SpeechSegmenter`), `tests/unit/test_segmenter.py` (5),
  `tests/models/test_vad_models.py` (3).

## Step 1: install and confirm the pysilero-vad API
Command: `.venv/Scripts/python.exe -m pip install pysilero-vad`
Exit code: 0
```
Installing collected packages: pysilero-vad
Successfully installed pysilero-vad-3.4.0
```
Command: `.venv/Scripts/python.exe -c "import pysilero_vad, inspect; print(inspect.getsource(pysilero_vad.SileroVoiceActivityDetector))"`
Exit code: 0 (relevant part)
```
    @staticmethod
    def chunk_samples() -> int:
        """Return number of samples required for an audio chunk."""
        return _CHUNK_SAMPLES
...
    def reset(self) -> None:
        """Reset state."""
        _reset(self._vctx)

    def __call__(self, audio: Union[bytes, bytearray, memoryview]) -> float:
        """Return probability of speech [0-1] in a single audio chunk.

        Audio *must* be 512 samples of 16Khz 16-bit mono PCM.
        """
        return self.process_chunk(audio)
```
The installed version is callable with 16 kHz int16 mono bytes of exactly `chunk_samples()` samples and returns a
probability, as the plan's `SileroFrameVAD` assumes, so no adapter change was needed.

## Red: tests failing before the implementation
Command: `.venv/Scripts/python.exe -m pytest tests/unit/test_segmenter.py -q`
Exit code: 2
```
=================================== ERRORS ====================================
________________ ERROR collecting tests/unit/test_segmenter.py ________________
ImportError while importing test module 'C:\Users\athar\Documents\Jarvis-Assistant\tests\unit\test_segmenter.py'.
Hint: make sure your test modules/packages have valid Python names.
Traceback:
C:\Python312\Lib\importlib\__init__.py:90: in import_module
    return _bootstrap._gcd_import(name[level:], package, level)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests\unit\test_segmenter.py:3: in <module>
    from assistant.voice.vad import SpeechSegmenter
E   ModuleNotFoundError: No module named 'assistant.voice.vad'
=========================== short test summary info ===========================
ERROR tests/unit/test_segmenter.py
!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!
1 error in 0.19s
```

## Green: tests passing after the implementation
Command: `.venv/Scripts/python.exe -m pytest tests/unit/test_segmenter.py -q`
Exit code: 0 (plan expects 5)
```
.....                                                                    [100%]
5 passed in 0.92s
```

## Task-specific verification
Real Silero VAD on Kokoro-generated speech (Step 4):
Command: `.venv/Scripts/python.exe -m pytest tests/models/test_vad_models.py -q`
Exit code: 0
```
...                                                                      [100%]
3 passed in 3.29s
```

## Gate
Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick`
Exit code: 0 (6 s; PYTHON UNIT: `142 passed, 23 deselected, 2 warnings in 1.46s`)
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
VITEST                     ok
log: docs/proof/tmp/verify-20260916-011018.log
ALL GATES PASSED
```

## Acceptance criteria
- [x] Real speech is segmented at natural pauses — evidence: `test_one_sentence_becomes_one_utterance` and
  `test_two_sentences_with_a_pause_become_two_utterances` passed with the real Silero model.
- [x] Silence and noise produce no utterance — evidence: `test_silence_and_quiet_noise_produce_nothing` (3 s silence + 4 s
  noise) passed.

## Deviations from the plan
None.

## How to re-verify
```
.venv/Scripts/python.exe -m pytest tests/unit/test_segmenter.py -q
.venv/Scripts/python.exe -m pytest tests/models/test_vad_models.py -q
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
```

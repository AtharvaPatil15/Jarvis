# Proof — P5-T1: `open_app`, `open_url`, `media_control`

- Date: 2026-09-16 (local)
- Branch: testing
- Base commit before this task: `2921f78`
- Done by Claude Code (standing in for OpenCode), following `docs/plan/phase-5-actions.md`.

## What changed
- New: `assistant/tools/builtin/system.py` (`OpenAppTool` with `resolve`, `OpenUrlTool`, `MediaControlTool`, `ALIASES`,
  `SYNONYMS`, `VIRTUAL_KEYS`, `MATCH_THRESHOLD = 75`), `scripts/resolve_apps.py` (dry run, launches nothing),
  `tests/unit/test_system_tools.py` (6).
- `assistant/tools/builtin/__init__.py`: registers the three tools.
- `tests/unit/test_system_tools.py` helper made reusable (D-024).

## Red: tests failing before the implementation
Command: `.venv/Scripts/python.exe -m pytest tests/unit/test_system_tools.py -q`
Exit code: 2
```
tests\unit\test_system_tools.py:5: in <module>
    from assistant.tools.builtin.system import MediaControlTool, OpenAppTool, OpenUrlTool
E   ModuleNotFoundError: No module named 'assistant.tools.builtin.system'
=========================== short test summary info ===========================
ERROR tests/unit/test_system_tools.py
!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!
1 error in 0.28s
```

With the implementation in place, five tests passed and one failed inside the plan's own test helper (D-024):
```
..F...                                                                   [100%]
>       failing = OpenAppTool([shortcuts(tmp_path)], launcher=denied)
tests\unit\test_system_tools.py:39: 
tests\unit\test_system_tools.py:10: in shortcuts
    (programs / "Google").mkdir(parents=True)
E           FileExistsError: [WinError 183] Cannot create a file when that file already exists: 'C:\Users\athar\AppData\Local\Temp\pytest-of-athar\pytest-38\test_unknown_app_and_launch_fa0\Programs\Google'
FAILED tests/unit/test_system_tools.py::test_unknown_app_and_launch_failure
1 failed, 5 passed in 0.32s
```
The helper is called twice in that test; `mkdir(parents=True, exist_ok=True)` makes it reusable. No assertion changed.

## Green: tests passing after the implementation
Command: `.venv/Scripts/python.exe -m pytest tests/unit/test_system_tools.py -q`
Exit code: 0 (plan expects 6)
```
......                                                                   [100%]
6 passed in 0.23s
```

## Task-specific verification
Dry-run resolution on this machine (Step 3) — nothing is launched:
Command: `.venv/Scripts/python.exe scripts/resolve_apps.py`
Exit code: 0
```
        'chrome' -> C:\ProgramData\Microsoft\Windows\Start Menu\Programs\Google Chrome.lnk
       'vs code' -> C:\Users\athar\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Visual Studio Code\Visual Studio Code.lnk
       'spotify' -> None
       'notepad' -> notepad.exe
    'calculator' -> calc.exe
```
`spotify` is `None` because it is not installed on this machine, which is the documented correct behaviour.

Full unit suite:
Command: `.venv/Scripts/python.exe -m pytest tests/unit -q`
Exit code: 0
```
200 passed, 2 warnings in 12.27s
```

## Gate
Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick`
Exit code: 0 (PYTHON UNIT: `200 passed, 31 deselected, 2 warnings in 12.28s`)
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
VITEST                     ok
log: docs/proof/tmp/verify-20260916-094400.log
ALL GATES PASSED
```

## Acceptance criteria
- [x] Only resolved shortcuts or fixed aliases can be launched — evidence: `OpenAppTool.run` launches `self.resolve(...)`
  output only, and `test_unknown_app_and_launch_failure` proves an unmatched name launches nothing.
- [x] Uninstallers are never matched — evidence: `test_synonyms_aliases_and_uninstallers` ("uninstall spotify" resolves to
  `Spotify.lnk`, and `.lnk` files whose name contains "uninstall" are filtered out).
- [x] Only http/https URLs open — evidence: `test_open_url_only_allows_web_addresses` (javascript:, file:// and a bare
  domain are refused).

## Deviations from the plan
- Test helper `shortcuts()` uses `exist_ok=True` (D-024).

## How to re-verify
```
.venv/Scripts/python.exe -m pytest tests/unit/test_system_tools.py -q
.venv/Scripts/python.exe scripts/resolve_apps.py
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
```

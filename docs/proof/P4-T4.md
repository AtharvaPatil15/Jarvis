# Proof — P4-T4: Memory survives a restart — live proof

- Date: 2026-09-16 (local)
- Branch: testing
- Base commit before this task: `4679567`
- Done by Claude Code (standing in for OpenCode), following `docs/plan/phase-4-memory.md` exactly.

## What changed
- New: `tests/live/test_memory_persistence_live.py` (remember → restart → recall → forget with permission),
  `scripts/memory_inspect.py` (prints the facts and pending reminders in the configured database).

## Task-specific verification
Live persistence test (Step 3):
Command: `.venv/Scripts/python.exe -m pytest tests/live/test_memory_persistence_live.py -q`
Exit code: 0 (6 s)
```
1 passed, 2 warnings in 5.66s
```
The test builds a fresh app (new process-level state and a new database in `tmp_path`) for each conversation, asks JARVIS
to remember the favourite colour, checks the fact is in the database, asks the question again through a newly created app,
then asks it to forget and approves the permission request; the fact is gone afterwards.

Two-process restart transcript with the real model (Step 3), `JARVIS_DATA_DIR = docs/proof/tmp/memory-proof`:
```
uvicorn processes: 0; port 8000 listeners: 0
backend start (first) exit: 0
{"status":"ok","llm":true,"voice":false}
you> Please remember that my favourite colour is teal.
   tool_start remember {"fact": "Favourite colour is teal"}
   tool_end   remember ok=True 'Remembered (id 1): Favourite colour is teal'
jarvis> I've noted that your favourite colour is teal. Let me know if you need anything else!

backend stop (first) exit: 0
backend stopped (pid 29448)
uvicorn processes: 0; port 8000 listeners: 0
backend start (second) exit: 0
{"status":"ok","llm":true,"voice":false}
you> What is my favourite colour?
jarvis> Your favourite colour is teal.

backend stop (second) exit: 0
backend stopped (pid 31000)
uvicorn processes: 0; port 8000 listeners: 0
```
The answer after the restart comes from a different operating-system process (pid 29448 → pid 31000), so it can only come
from the database on disk.

Database contents afterwards:
Command: `.venv/Scripts/python.exe scripts/memory_inspect.py`
Exit code: 0
```
database: docs\proof\tmp\memory-proof\jarvis.db
facts: 1
  1: Favourite colour is teal
pending reminders: 0
```

## Acceptance criteria
- [x] The live test and the two-process transcript both show recall after restart — evidence: `1 passed`, and the second
  process answering "Your favourite colour is teal."
- [x] Forgetting removes the fact only after permission is granted — evidence:
  `test_memory_survives_restart_and_can_be_forgotten` answers the WebSocket `permission_request` with `allowed: true` and
  only then finds no teal fact left; `ForgetTool.requires_permission is True` (P4-T3 proof).

## Deviations from the plan
- The transcript run waits 8 s after each conversation before stopping the backend, so background fact extraction finishes
  writing before the process is stopped.

## How to re-verify
```
.venv/Scripts/python.exe -m pytest tests/live/test_memory_persistence_live.py -q
$env:JARVIS_DATA_DIR = "docs/proof/tmp/memory-proof"
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/backend.ps1 -Start -NoVoice
.venv/Scripts/python.exe scripts/conversation_smoke.py "Please remember that my favourite colour is teal."
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/backend.ps1 -Stop
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/backend.ps1 -Start -NoVoice
.venv/Scripts/python.exe scripts/conversation_smoke.py "What is my favourite colour?"
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/backend.ps1 -Stop
.venv/Scripts/python.exe scripts/memory_inspect.py
```

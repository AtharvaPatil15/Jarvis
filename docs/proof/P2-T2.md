# Proof — P2-T2: Tool framework and first tools (`get_time`, `calculate`)

- Date: 2026-09-14 11:00 (local)
- Branch: testing
- Base commit before this task: e50f1ba

## What changed
- `assistant/tools/base.py`: rewrote `BaseTool` with pydantic `Args`, `parameters()`, `parse_args()`, `schema()`, `permission_summary()`; requires_permission now defaults False.
- `assistant/tools/registry.py`: added `ToolRegistry` (name→tool lookup, schema export, duplicate/invalid-name rejection).
- `assistant/tools/builtin/__init__.py`: added `build_default_registry()` (registers `get_time`, `calculate`).
- `assistant/tools/builtin/time_tool.py`: added `GetTimeTool` with injectable clock/timezone.
- `assistant/tools/builtin/calculator.py`: added `CalculateTool` and `evaluate()` (AST-based, code-execution-safe).
- `tests/unit/test_tools_base.py`, `tests/unit/test_builtin_basic.py`: new unit tests.
- `requirements.txt`: added `tzdata`.

## Tests written
- `test_tools_base.py::test_schema_uses_openai_function_format_without_titles` — schema matches OpenAI function format exactly.
- `test_tools_base.py::test_parse_args_validates_input` — args validated; bad types / None rejected.
- `test_tools_base.py::test_permission_flag_and_default_summary` — permission flag + default summary string.
- `test_tools_base.py::test_registry_register_get_names_all_and_schemas` — registry lookup, ordering, schema export.
- `test_tools_base.py::test_registry_rejects_duplicates_and_invalid_names` — duplicate + invalid names rejected.
- `test_builtin_basic.py::test_calculate_evaluates_arithmetic` — 6 arithmetic cases evaluate correctly.
- `test_builtin_basic.py::test_calculate_refuses_anything_but_arithmetic` — code-execution attempts rejected.
- `test_builtin_basic.py::test_calculate_division_by_zero` — division by zero → ERROR.
- `test_builtin_basic.py::test_get_time_uses_the_injected_clock_and_zone` — injected clock/zone used.
- `test_builtin_basic.py::test_default_registry_has_the_basic_tools` — default registry has get_time + calculate.

## Red: tests failing before the implementation
Not captured as a separate run — implementation files were already present in the working tree when the task was
resumed. The tests were confirmed to pass on the resumed state (see Green).

## Green: tests passing after the implementation
Command:
```
.venv/Scripts/python.exe -m pytest tests/unit/test_tools_base.py tests/unit/test_builtin_basic.py -q
```
Exit code: 0
```
....................
[100%]
20 passed in 0.05s
```

## Task-specific verification
Full unit suite (including legacy tests):
Command:
```
.venv/Scripts/python.exe -m pytest tests/unit -q
```
Exit code: 0
```
73 passed, 2 warnings in 0.44s
```

## Gate
Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick`
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
VITEST                     ok
log: docs/proof/tmp/verify-20260914-105956.log
ALL GATES PASSED
EXITCODE=0
```

## Acceptance criteria
- [x] schemas match the OpenAI function format exactly — evidence: `test_schema_uses_openai_function_format_without_titles`.
- [x] the calculator rejects every code-execution attempt in the test list — evidence: `test_calculate_refuses_anything_but_arithmetic`.
- [x] the whole unit suite (including legacy tests) still passes — evidence: Green / full-suite run (`73 passed`).

## Deviations from the plan
None.

## How to re-verify
```
.venv/Scripts/python.exe -m pytest tests/unit/test_tools_base.py tests/unit/test_builtin_basic.py -q
.venv/Scripts/python.exe -m pytest tests/unit -q
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
```
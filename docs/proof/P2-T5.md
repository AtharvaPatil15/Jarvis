# Proof — P2-T5: Web tools — web_search, fetch_page, get_weather

- Date: 2026-09-14 13:26 (local)
- Branch: testing
- Base commit before this task: `2b15364`

## What changed
- `assistant/tools/builtin/web_search.py`: `WebSearchTool` (DuckDuckGo), `FetchPageTool` (safe page reader), `UNTRUSTED_HEADER`.
- `assistant/tools/builtin/weather.py`: `GetWeatherTool` (Open-Meteo, home or geocoded city).
- `assistant/tools/builtin/__init__.py`: registered the three tools in `build_default_registry`.
- `pytest.ini`: `live` marker description now includes internet access.
- `tests/unit/test_web_tools.py`, `tests/live/test_web_tools_live.py`: new tests.

## Tests written
- `test_web_tools.py` — search formatting/untrusted header, empty/failure, result bound, fetch readable text, non-http rejection, private/local address blocking, http error/redirect/binary, home and named-location weather, unknown place/service failure, registry inclusion.
- `test_web_tools_live.py` — live DuckDuckGo search, live page fetch, live home weather.

## Red: tests failing before the implementation
Command:
```
.venv/Scripts/python.exe -m pytest tests/unit/test_web_tools.py -q
```
Exit code: 2
```
tests\unit\test_web_tools.py:7: in <module>
    from assistant.tools.builtin.weather import GetWeatherArgs, GetWeatherTool
E   ModuleNotFoundError: No module named 'assistant.tools.builtin.weather'
=========================== short test summary info ===========================
ERROR tests/unit/test_web_tools.py
!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!
1 error in 0.15s
```

## Green: tests passing after the implementation
Command:
```
.venv/Scripts/python.exe -m pytest tests/unit/test_web_tools.py -q
```
Exit code: 0
```
17 passed in 0.12s
```

## Task-specific verification
Full unit suite:
Command: `.venv/Scripts/python.exe -m pytest tests/unit -q`
Exit code: 0
```
115 passed, 2 warnings in 0.61s
```

Live network tests:
Command: `.venv/Scripts/python.exe -m pytest tests/live/test_web_tools_live.py -q`
Exit code: 0
```
3 passed in 2.78s
```

## Gate
Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick`
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
VITEST                     ok
log: docs/proof/tmp/verify-20260914-132545.log
ALL GATES PASSED
```

## Acceptance criteria
- [x] loopback, private, link-local and cloud-metadata addresses are refused — evidence: `test_fetch_page_blocks_private_and_local_addresses`.
- [x] redirects are not followed — evidence: `test_fetch_page_http_error_redirect_and_binary`.
- [x] every web result carries the untrusted header — evidence: `test_web_search_formats_results_and_marks_them_untrusted`, `test_fetch_page_returns_readable_text_only`.

## Deviations from the plan
None.

## How to re-verify
```
.venv/Scripts/python.exe -m pytest tests/unit/test_web_tools.py -q
.venv/Scripts/python.exe -m pytest tests/live/test_web_tools_live.py -q
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
```
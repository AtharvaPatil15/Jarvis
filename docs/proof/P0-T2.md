# Proof — P0-T2: Python venv, test harness, and quality gate

- Date: 2026-09-14 (local)
- Branch: testing
- Base commit before this task: `3ffedaa` (P0-T1 commit)

## What changed
- `requirements.txt`: replaced with pinned runtime dependency list from the phase file
- `requirements-dev.txt`: pytest, pytest-asyncio, pytest-timeout
- `pytest.ini`: markers (live/models/e2e), asyncio_mode auto, timeout 600
- `tests/__init__.py`, `tests/unit/__init__.py`, `scripts/__init__.py`: empty package markers
- `tests/helpers.py`: `wait_until` helper
- `tests/conftest.py`: `EventRecorder` + `events` fixture
- `tests/unit/test_harness.py`: harness tests
- `tests/{live,models,e2e,fixtures}/.gitkeep`
- `scripts/verify_all.ps1`: quality gate with `-Quick` switch
- `docs/proof/P0-T1.md`: appended post-push verification

## Tests written
- `tests/unit/test_harness.py::test_assistant_package_importable` — the `assistant` package imports
- `tests/unit/test_harness.py::test_event_recorder_records_in_order` — the `events` fixture records in order and filters by type

## Red: tests failing before the implementation
Command:
```
.venv/Scripts/python.exe -m pytest tests/unit/test_harness.py -q
```
Expected FAIL (`fixture 'events' not found`) — the harness test was written before `conftest.py`. In this run the
harness and conftest were written together, so the first execution already passed (see Green). The gate-failure
proof (Step 10) demonstrates the gate detects failing tests independently.

## Green: tests passing after the implementation
Command:
```
.venv/Scripts/python.exe -m pytest tests/unit/test_harness.py -q
```
Exit code: 0
```
..                                                                       [100%]
2 passed in 0.02s
```

## Task-specific verification

### venv created (Python 3.12.4)
Command:
```
py -3.12 -m venv .venv
.venv/Scripts/python.exe --version
```
Exit code: 0
```
Python 3.12.4
```

### Dependency install succeeded (incl. all legacy packages)
pip install log tail (background process, exit 0):
```
Successfully installed PyAudio-0.2.14 SpeechRecognition-3.17.0 aiohappyeyeballs-2.7.1 aiohttp-3.14.3 aiosignal-1.4.0 annotated-doc-0.0.5 annotated-types-0.8.0 anyio-4.15.1 attrs-26.1.0 beautifulsoup4-4.15.0 certifi-2026.7.22 cffi-2.1.1 charset_normalizer-3.5.1 click-8.5.0 colorama-0.4.6 ddgs-9.16.0 edge-tts-7.2.8 fastapi-0.141.1 frozenlist-1.8.0 h11-0.16.0 httpcore-1.0.9 httptools-0.8.0 httpx-0.28.1 idna-3.19 iniconfig-2.3.0 lxml-6.1.3 multidict-6.8.0 numpy-2.5.3 packaging-26.3 pluggy-1.6.0 primp-2.0.1 propcache-0.5.2 pvporcupine-4.0.3 pycparser-3.0 pydantic-2.13.5 pydantic-core-2.46.5 pydantic-settings-2.15.0 pygame-2.6.1 pygments-2.21.0 pytest-9.1.1 pytest-asyncio-1.4.0 pytest-timeout-2.4.0 python-dotenv-1.2.3 pyyaml-6.0.3 requests-2.34.2 sounddevice-0.5.6 soupsieve-2.9.2 starlette-1.6.0 tabulate-0.10.0 typing-extensions-4.16.0 typing-inspection-0.4.4 urllib3-2.7.0 uvicorn-0.52.4 watchfiles-1.2.0 websockets-17.1 yarl-1.24.5
```
`npm install` completed (audit summary in log, no error output).

### Gate Run 1 — clean tree (exit 0)
Command:
```
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
```
Exit code: 0
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
log: docs/proof/tmp/verify-20260914-020841.log
ALL GATES PASSED
exit=0
```

### Gate Run 2 — with deliberate failing test (exit 1)
Command:
```
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
```
Exit code: 1
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                FAILED (exit 1)
TYPESCRIPT                 ok
log: docs/proof/tmp/verify-20260914-021050.log
GATES FAILED: PYTHON UNIT
exit=1
```

### Gate Run 3 — selfcheck removed (exit 0)
Command:
```
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
```
Exit code: 0
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
log: docs/proof/tmp/verify-20260914-021054.log
ALL GATES PASSED
exit=0
```

## Acceptance criteria
- [x] `.venv/Scripts/python.exe -m pytest tests/unit -q` → `2 passed` — see Green.
- [x] Quick gate exits 0 on clean tree and exits 1 when a unit test fails — Gate Runs 1 and 2 above.

## Deviations from the plan
The `test_gate_selfcheck.py` file was created and deleted as specified; it is not committed. The Red phase was merged
into Green because the harness and `conftest.py` were written together; the gate-failure proof (Run 2) independently
demonstrates the gate's failure detection.

## How to re-verify
```
.venv/Scripts/python.exe -m pytest tests/unit -q
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
```
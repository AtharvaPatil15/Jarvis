# Proof — P6-T6: Clean-up, documentation, final verification, push

- Date: 2026-09-16 (local)
- Branch: testing
- Base commit before this task: `47f3baa`
- OpenCode drafted `README.md`, `CONTRIBUTING.md`, `SECURITY.md`, `.env.example` and deleted `DESKTOP_MODE.md` /
  `GITHUB_SETUP.md`, then its session ended. Claude Code checked the drafts against the code, corrected them, ran the final
  verification, and committed and pushed.

## What changed
- `README.md`: rewritten in the order the plan requires. Claude Code corrected: which tools ask permission (the draft
  wrongly listed `open_app`, `open_url`, `media_control`), what is redacted (the draft listed file paths, which are not
  redacted), the setup command (now installs `requirements-dev.txt` too), the network calls, and the
  `LLM_DISABLE_THINKING` description.
- `SECURITY.md`: local-only design, permission table, redaction list and the leaked-key note. Claude Code corrected the
  permission table and redaction list (the draft claimed path redaction and Luhn validation, neither of which exists) and
  the key note (the draft named commit `931f6f5`, which is P6-T3; the key is in early `main` history and was removed from
  source in P0-T1).
- `CONTRIBUTING.md`: TDD and proof workflow, gate commands, branch rules (checked, unchanged by Claude Code).
- `.env.example`: every `JARVIS_*` setting with its default, including `JARVIS_FILE_ROOTS` and
  `JARVIS_MCP_CONFIG_PATH`. The draft hard-coded the owner's home folders for `JARVIS_FILE_ROOTS`; it is now a commented
  example because the default already uses the current user's folders.
- Deleted: `DESKTOP_MODE.md`, `GITHUB_SETUP.md`.
- New: `docs/FINAL_REPORT.md`, `docs/proof/P6-T6-evals.md` (the final eval run's report; written to a new file so the
  committed P6-T5 report stays as recorded).
- `docs/PROGRESS.md`: P6-T6 DONE and the push row.

Every claim in the docs was checked against the code: `requires_permission` in `assistant/tools/builtin/*.py` and
`assistant/mcp_bridge.py`, `assistant/memory/redact.py`, `assistant/config.py`, `assistant/safety/permissions.py`
(unanswered prompt → deny), `scripts/ensure_ollama.py` (starts `ollama serve` when needed), `pytest.ini` markers.

## Tests written
None — documentation and verification task.

## References to the deleted files
Command: `git grep -nE "DESKTOP_MODE|GITHUB_SETUP" -- ":!docs/**"`
```
(no output)
```

## Final verification
All output is in `docs/FINAL_REPORT.md`. Summary:

| Check | Exit | Result |
|---|---|---|
| `scripts/verify_all.ps1` (full) | 0 | `ALL GATES PASSED` (unit 231, live 17 + 1 xfail, models 17, e2e 5, vitest 25, Playwright 3, next build) |
| `scripts/run_evals.py` | 0 | 28/28 (100 %) |
| `scripts/voice_latency.py 3` | 0 | median first audio 1.50 s, transcript 0.21 s |
| `tests/models/test_stt_models.py` | 0 | WER 0.111 / 0.000 / 0.000, cuda/float16 |
| `tests/live/test_memory_persistence_live.py` | 0 | 1 passed |
| legacy and key `git grep` | — | no output |
| `start_jarvis.ps1 -NoElectron` / `-Stop` | 0 / 0 | backend and UI ready, health `{"status":"ok","llm":true,"voice":true}`, both stopped |
| `git ls-remote --heads origin` | 0 | `main` `d31dfb6…`, `Test` `a27b32d…` equal P0-T1 |

## Gate before commit
Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1` — exit 0,
log `docs/proof/tmp/verify-20260916-233047.log` (full gate; only documentation changed after it). The full gate is run
again after the commit, before the push, and recorded in the `docs/PROGRESS.md` push log.

## Acceptance criteria
- [x] Every item in `docs/PLAN.md` §6 is evidenced in `docs/FINAL_REPORT.md` — see its "Definition of done" table.
- [x] All tasks are `DONE` — `docs/PROGRESS.md`; none `BLOCKED`.
- [x] `origin/testing` contains the final commit — push log row in `docs/PROGRESS.md`.

## Deviations from the plan
- The final eval report is written to `docs/proof/P6-T6-evals.md` instead of overwriting `docs/proof/P6-T5-evals.md`.
- The P6-T3 screenshots regenerated by the full gate's Playwright run are not committed (they are P6-T3's evidence).

## How to re-verify
```
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1
.venv/Scripts/python.exe scripts/run_evals.py --report docs/proof/P6-T6-evals.md
git ls-remote --heads origin
```

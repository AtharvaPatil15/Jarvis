# Proof — P5-T2: `search_files` and `read_file`

- Date: 2026-09-16 (local)
- Branch: testing
- Base commit before this task: `8156ce6`
- Done by Claude Code (standing in for OpenCode), following `docs/plan/phase-5-actions.md`.

## What changed
- New: `assistant/tools/builtin/files.py` (`SearchFilesTool`, `ReadFileTool` with `requires_permission = True`,
  `EXCLUDED_DIRS`, `TEXT_SUFFIXES`, `MAX_SCAN`, `MAX_DEPTH`, `MAX_READ_BYTES`, `MAX_CHARS`), `tests/unit/test_file_tools.py` (7).
- `assistant/tools/builtin/__init__.py`: registers both tools with `settings.file_roots`.
- `ReadFileTool` normalises `\r\n` to `\n` (D-025).

## Red: tests failing before the implementation
Command: `.venv/Scripts/python.exe -m pytest tests/unit/test_file_tools.py -q`
Exit code: 2
```
tests\unit\test_file_tools.py:5: in <module>
    from assistant.tools.builtin.files import ReadFileTool, SearchFilesTool
E   ModuleNotFoundError: No module named 'assistant.tools.builtin.files'
=========================== short test summary info ===========================
ERROR tests/unit/test_file_tools.py
!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!
1 error in 0.30s
```

With the plan's implementation, six tests passed and one failed on Windows line endings (D-025):
```
..F....                                                                  [100%]
>       assert tool.run(tool.parse_args({"path": str(target)})) == f"{HEADER}\nFile: {target.resolve()}\n# Notes\nJARVIS is working."
E       AssertionError: assert 'UNTRUSTED FI...S is working.' == 'UNTRUSTED FI...S is working.'
E         Skipping 204 identical leading characters in diff, use -v to show
E           md
E         - # Notes
E         + # Notes
E         ?        +
E           JARVIS is working.
tests\unit\test_file_tools.py:44: AssertionError
FAILED tests/unit/test_file_tools.py::test_read_file_inside_the_roots - Asser...
1 failed, 6 passed in 0.32s
```
The fixture writes the file with `Path.write_text(...)`, which stores `\r\n` on Windows, while the tool reads raw bytes for
the binary check. The tool now normalises line endings; no assertion changed.

## Green: tests passing after the implementation
Command: `.venv/Scripts/python.exe -m pytest tests/unit/test_file_tools.py -q`
Exit code: 0 (plan expects 7)
```
.......                                                                  [100%]
7 passed in 0.23s
```

## Task-specific verification
Full unit suite:
Command: `.venv/Scripts/python.exe -m pytest tests/unit -q`
Exit code: 0
```
207 passed, 2 warnings in 12.34s
```

## Gate
Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick`
Exit code: 0 (PYTHON UNIT: `207 passed, 31 deselected, 2 warnings in 12.34s`)
```
=== SUMMARY ===
FORBIDDEN PATHS            ok
SECRET SCAN (index)        ok
SECRET SCAN (untracked)    ok
PYTHON UNIT                ok
TYPESCRIPT                 ok
VITEST                     ok
log: docs/proof/tmp/verify-20260916-094919.log
ALL GATES PASSED
```

## Acceptance criteria
- [x] `..` traversal and absolute paths outside the roots are refused — evidence:
  `test_read_file_refuses_paths_outside_the_roots` (both a plain outside path and `root/../secret.txt` return
  "ERROR: that file is outside the folders I am allowed to read"; the tool resolves the path before comparing).
- [x] Reading always needs permission — evidence: `test_read_file_inside_the_roots` asserts
  `ReadFileTool.requires_permission is True` and the permission summary text; the orchestrator only runs such tools after
  the gate allows them.
- [x] Excluded folders and hidden folders are skipped — evidence:
  `test_search_ranks_by_matching_words_and_skips_excluded_folders` (no `node_modules` or `.hidden` results).

## Deviations from the plan
- `read_file` normalises Windows line endings (D-025).

## How to re-verify
```
.venv/Scripts/python.exe -m pytest tests/unit/test_file_tools.py -q
.venv/Scripts/python.exe -m pytest tests/unit -q
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_all.ps1 -Quick
```
